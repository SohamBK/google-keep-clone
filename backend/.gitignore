# Node.js
node_modules/
dist/
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# OS and IDE specific files
.DS_Store
.vscode/
.idea/
*.suo
*.user
*.bak
*.log
Thumbs.db

# Docker
.dockerignore
# Docker images generated by the build process are not committed
# The Dockerfile itself is committed, but not the built image.

# Swagger (if generated dynamically and not committed)
# If you are generating openapi.json dynamically and don't want to commit it, uncomment the line below:
# swagger/openapi.json

# Test coverage reports
coverage/

# Environment variables
.env
.env.local
.env.development
.env.test
.env.production